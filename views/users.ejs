<!DOCTYPE html>
<html>
  <head>
    <title><%= title %></title>
    <link rel='stylesheet' href='/stylesheets/style.css' />
  </head>
  <body>
    <h1><%= title %></h1>
    <p>User template <%= title %></p>
    <% for(var i=0; i < users.length; i++) { %>
        <div>
 
         <p>Name:<%= users[i].id %></p>
         <p>ID: <%= users[i].name %></p>
       </div>
    <% } %>
  </body>
</html>
